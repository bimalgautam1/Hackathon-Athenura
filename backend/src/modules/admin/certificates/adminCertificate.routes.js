/**
  adminCertificate.routes.js
  Defines Express routes for adminCertificate domain.
 */
import { Router } from 'express'

const router = Router()

// Admin certificate routes go here.
// This router is intentionally stubbed until certificate endpoints are implemented.

export default router

